<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>✨ User Manager | All Users</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen">
    <div class="max-w-7xl mx-auto p-10">
        <div class="mb-6">
            <a href="/" class="text-blue-400 hover:text-blue-500 transition-colors">&larr; Back to Home</a>
        </div>

        <h1 class="text-4xl font-bold tracking-tight mb-8 text-center text-white">All Users</h1>

        <% if (users.length > 0) { %>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                <% users.forEach(function(e) { %>
                    <div class="bg-zinc-800 rounded-2xl shadow-lg overflow-hidden hover:scale-105 transform transition-all">
                        <div class="w-full h-56 bg-zinc-700 overflow-hidden">
                            <img 
                                src="<%= e.image || 'https://via.placeholder.com/200' %>" 
                                alt="<%= e.name %>" 
                                class="w-full h-full object-cover"
                            >
                        </div>
                        <div class="p-4">
                            <h3 class="text-xl font-semibold mb-1"><%= e.name %></h3>
                            <p class="text-zinc-400 mb-3"><%= e.email %></p>
                            <div class="flex gap-3">
                                <a href="/edit/<%= e._id %>" class="text-blue-400 hover:text-blue-500 transition-colors font-medium">Edit</a>
                                <a href="/delete/<%= e._id %>" class="text-red-500 hover:text-red-600 transition-colors font-medium">Delete</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <h2 class="text-xl text-zinc-400 mt-20 text-center">🚫 No users added yet</h2>
        <% } %>
    </div>
</body>
</html>
