<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>✨ User Manager | Update User</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center">
    <div class="w-full max-w-lg p-10 bg-zinc-800 rounded-2xl shadow-xl">
        <div class="mb-8">
            <a href="/read" class="text-blue-400 hover:text-blue-500 transition-colors">&larr; View All Users</a>
        </div>

        <h1 class="text-4xl font-bold tracking-tight mb-6 text-center">Update User</h1>
        
        <form action="/update/<%= user._id %>" method="post" class="flex flex-col gap-4">
            <input 
                class="px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-lg focus:outline-none focus:border-blue-500 transition-all" 
                type="text" 
                placeholder="Name" 
                name="name" 
                value="<%= user.name %>" 
                required
            >
            <input 
                class="px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-lg focus:outline-none focus:border-blue-500 transition-all" 
                type="email" 
                placeholder="Email" 
                name="email" 
                value="<%= user.email %>" 
                required
            >
            <input 
                class="px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-lg focus:outline-none focus:border-blue-500 transition-all" 
                type="text" 
                placeholder="Image URL" 
                name="image" 
                value="<%= user.image %>"
            >
            <button 
                type="submit" 
                class="mt-4 px-5 py-3 bg-blue-500 rounded-lg font-semibold hover:bg-blue-600 transition-colors"
            >
                Update User
            </button>
        </form>
    </div>
</body>
</html>
